import material/material;
import sys/system;
import string;

main(){
	str = readBytes(4);
	arrNum = s2a2(str, " ", fromDigit);
	println(arrNum);
	arrStr = map(arrNum, \i -> i2s(i));
	println(arrStr);
	quit(0);
}